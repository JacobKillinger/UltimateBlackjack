import java.beans.Visibility;

public class Main {
    public static void main(String[] args) {
        BlackjackController controller = new BlackjackController(); //Creates controller and initiates the view from the controller
        controller.startView();
    }
}